NODE_ENV="production"
# For Docker Compose (use service name as host):
DATABASE_URL="postgresql://postgres:PASSWORD@db:5432/uni-feedback"
# For local development (use localhost):
# DATABASE_URL="postgresql://postgres:PASSWORD@localhost:5433/uni-feedback"
PORT=3000