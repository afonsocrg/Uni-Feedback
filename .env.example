# =============================================================================
# SHARED INFRASTRUCTURE ENVIRONMENT VARIABLES
# =============================================================================
# Copy this file to .env and update with your values
# These variables are shared across all apps and packages

# Database Configuration
# For local development, use localhost:5433 (Docker Compose exposes port 5433)
# For Docker Compose services, use db:5432 (internal Docker network)
DATABASE_URL="postgresql://postgres:YOUR_PASSWORD@localhost:5433/uni-feedback"
PGPASSWORD=YOUR_PASSWORD

# Node Environment (can be overridden by individual apps)
NODE_ENV="development"
